<div id="<%= dom_id operation %>">

<% if operation.otype == "outcome" %>
    <div class = "one_item_outcome">
<% else %>
    <div class = "one_item_income">
<% end %>
    <table>
        <tr class = "one_item">
            <td class = "op_odate"><%= operation.odate.to_date.strftime('%d.%m.%Y') %></td>
            <td class = "op_amount"><%= operation.amount %></td>
            <td class = "op_category"><%= Category.find(operation.category_id).name %></td>
            <td class = "op_button"><button type="button">
                <%= link_to '<i class="bi bi-file-earmark-text-fill"></i>'.html_safe, operation %>
            </button></td>
            <td class = "op_button"><button type="button">
                <%= link_to '<i class="bi bi-pencil-fill"></i>'.html_safe, edit_operation_path(operation) %>
            </button></td>
            <td class = "op_button"><button type="button">
                <%= link_to '<i class="bi bi-trash3-fill"></i>'.html_safe, operation, data: { turbo_method: :delete, turbo_confirm: 'Точно видалити?' } %>
            </button></td>
        </tr>
    </table>
</div>

</div>
